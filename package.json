{
  "name": "portfolio-site",
  "version": "1.0.0",
  "description": "최명원 포트폴리오 — 프론트엔드/백엔드 분리 구조",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace backend",
    "start": "npm run start --workspace backend"
  },
  "engines": {
    "node": ">=20"
  }
}
