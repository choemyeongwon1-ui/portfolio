# PROJECT CONTEXT — 포트폴리오 분야 필터·디자인

이 문서는 대화가 바뀌어도 같은 기준으로 작업하기 위한 맥락 파일이다.
작업 전에 이 문서와 실제 파일(index.html, style.css, script.js)을 먼저 읽고,
바뀐 결정만 이 문서에 갱신한다.

---

## 1. 배경

- 대상: 최명원(그린스마트시티학과) 기업 지원용 포트폴리오 웹사이트
- 형태: 프론트엔드(정적 페이지 + ES 모듈) / 백엔드(Node.js + Express API) 분리 구조, 빌드 도구 없음
  - 2026-09-22 이전에는 단일 페이지(index.html + style.css + script.js)였다. 14절 참고.
- 이번 작업 목표: 프로젝트 섹션에 **분야별 필터**와 **문서형 디자인**을 정리해 넣는 것
- 톤: 장식형이 아니라 정보 전달형. 기업 담당자가 한눈에 읽을 수 있어야 한다.

---

## 2. 현재 상태 (2026-09-22 기준)

| 항목 | 상태 |
|---|---|
| 페이지 구조 | 홈 / 소개 / 기술 / 프로젝트 / 연락처 5개 섹션 |
| 내용의 출처 | `backend/src/data/*.json` → API → 화면. HTML에는 내용이 없다 |
| 프로젝트 섹션 | `.portfolio-doc` 문서형 블록으로 구현됨 |
| 필터 탭 | `#filterBar`에 `/api/categories` 결과로 생성. 클릭 시 서버에 해당 분야만 요청 |
| 상세 보기 | `.proj-toggle` 버튼으로 카드별 펼침/접기 동작 |
| 카드 수 | 3개 (웹 1 · 데이터 1 · 디자인 1) |
| 우측 보조 영역 | 제거됨 (11절 결정) |

이미 작동하는 기능이므로, 다음 작업에서 **삭제하거나 다시 만들지 않는다.**

---

## 3. 분야 분류 기준

| 분야 | data-category | 포함 범위 |
|---|---|---|
| 전체 | `all` | 모든 프로젝트 |
| 웹 | `web` | 웹·서비스·디지털 인터페이스 |
| 데이터 | `data` | 분석, 시각화, 공공데이터, 공간정보(GIS) |
| 디자인 | `design` | 시각디자인, UX/UI, 정보구조 |

- 한 카드는 현재 **하나의 분야만** 가진다(`data-category` 단일 값).
- 복수 분야가 필요해지면 그때 `data-category="web data"` 형태로 확장하고 이 문서를 갱신한다.

---

## 4. 카드 원문 (현재 index.html에 있는 사실)

각 카드는 **공식 사이트 링크 1개**를 함께 가진다. 링크는 새 탭에서 열린다.

### 1) 상명대학교 홈페이지 살펴보기 — 웹
- 한 줄: 대학 공식 홈페이지의 메뉴 구성과 정보 동선을 정리한 기록
- 키워드: 대학 홈페이지 / 정보 구조 / 웹 탐색
- 문제: 상위 메뉴가 여러 갈래라 처음 방문하면 항목 위치를 찾기 어렵다
- 접근: 자주 찾는 정보를 기준으로 상단 메뉴를 따라가며 분류를 정리
- 결과: 공지사항·학사일정으로 이어지는 기본 탐색 동선 확인
- 링크: https://www.smu.ac.kr/

### 2) 그린스마트시티학과 교육과정 정리 — 데이터
- 한 줄: 소속 학과가 다루는 분야와 교육과정을 확인해 정리한 기록
- 키워드: 그린스마트시티 / 공간정보 / 교육과정
- 문제: 학과 범위가 환경·도시와 디지털 기술에 걸쳐 있어 한 문장으로 설명하기 어렵다
- 접근: 학과 안내·교육과정·진로 안내를 순서대로 확인해 분야를 구분
- 결과: 환경 기반 위에 데이터·디지털 기술이 결합된 교육 방향 확인
- 링크: https://greensmartcity.smu.ac.kr/greensmartcity/index.do

### 3) 천안캠퍼스 학술정보관 이용 안내 정리 — 디자인
- 한 줄: 도서관의 자료 검색과 시설 이용 흐름을 읽기 쉽게 정리한 기록
- 키워드: 학술정보관 / 자료 검색 / 정보 구조
- 문제: 통합검색·전자정보·시설 안내가 서로 다른 메뉴에 있어 경로를 찾기 어렵다
- 접근: 자료를 찾는 경우와 공간을 이용하는 경우로 목적을 나눠 정리
- 결과: 통합검색 → 소장 확인 → 시설 이용으로 이어지는 흐름 정리
- 링크: https://libnt.smuc.ac.kr/

> **분류 근거**: 홈페이지는 웹 인터페이스 탐색이라 `web`,
> 학과는 공간정보·데이터 기반 교육과정을 다루므로 `data`,
> 도서관은 정보 구조와 이용 흐름 정리라 `design` 으로 두었다.
> 세 분야에 하나씩 배치해 필터 탭이 모두 동작하는 것을 확인할 수 있게 했다.

> **사실 범위**: 위 원문에 없는 수치(이용자 수, 절감률), 수상 이력, 사용하지 않은 기술은
> 추가하지 않는다. 필요하면 "확인 필요"로 표시한다.
> 세 카드의 내용은 공식 사이트에서 확인 가능한 사실과, 그 사이트를 살펴보고 정리했다는
> 범위까지만 적는다. 직접 만들거나 개발했다는 표현은 쓰지 않는다.

---

## 5. 필터 동작 명세

| 방문자 행동 | 보일 결과 | 유지할 것 |
|---|---|---|
| 페이지 진입 | 전체 선택 상태, 카드 3개 | 원래 카드 순서 |
| 웹 선택 | 웹 카드 1개만 | 카드 문구·링크 |
| 데이터 선택 | 데이터 카드 1개만 | 다른 탭의 스타일 |
| 디자인 선택 | 디자인 카드 1개만 | 필터 바 위치 |
| 결과 0개인 분야 선택 | 안내 문구 표시 | 필터 바와 탭 상태 |
| 전체로 복귀 | 다시 3개, 원래 순서 | 카드 내용 |

- 탭은 항상 하나만 활성(`.is-active`).
- 전환은 부드럽되 과한 애니메이션은 쓰지 않는다.
- 카드 개수 표시(`프로젝트 N개`)는 선택된 분야의 개수와 일치해야 한다.

---

## 6. 상세 설명 동작 명세

| 방문자 행동 | 보일 결과 | 유지할 것 |
|---|---|---|
| 자세히 보기 누름 | 그 카드의 문제·접근·결과만 펼침 | 다른 카드의 열림/닫힘 상태 |
| 다시 누름 | 설명 접힘, 버튼 문구 "자세히 보기"로 복귀 | 현재 필터 선택 상태 |
| 열린 카드가 필터로 숨겨짐 | 숨김 처리만, 상태는 유지 | 필터 결과의 정확성 |

- 상세 순서는 **문제 → 접근 → 결과** 고정.
- 버튼 문구는 열림 상태에서 "접기", 닫힘 상태에서 "자세히 보기".
- `aria-expanded` 값을 상태와 함께 갱신한다.

---

## 7. 디자인 기준

현재 사이트는 연녹색 자연 친화 테마를 쓰고 있다. 프로젝트 섹션만 **문서형**으로 정리하되,
사이트 전체 팔레트와 충돌하지 않게 기존 CSS 변수를 그대로 사용한다.

| 역할 | 변수 | 값 |
|---|---|---|
| 본문 배경 | `--bg` | #F5FAE8 |
| 카드 배경 | `--bg-card` | #FDFFF7 |
| 강조(선택 탭) | `--primary` / `--primary-dk` | #6A9A52 / #527a3f |
| 본문 텍스트 | `--text` | #3a5230 |
| 보조 텍스트 | `--text-sub` | #6a8a5a |
| 경계선 | `--border`, `--border-g` | 연녹색 반투명 |

### 레이아웃 원칙
- 카드는 기본 3열, 950px 이하에서 1열(기존 반응형 배치를 그대로 유지).
- 여백을 넉넉히, 카드 간 간격을 충분히 둔다.
- 과한 그림자·과한 둥근 모서리·과한 애니메이션을 피한다.
- 상단에 섹션명 → 한 줄 소개 → 구분선 순서의 문서형 머리 영역을 유지한다.

### 문체
- 공식적이되 부담스럽지 않게, 짧고 분명한 문장.
- 기획서형 포트폴리오 느낌.

---

## 8. 유지 조건 (바꾸지 말 것)

- 홈·소개·기술·연락처 섹션의 문구, 구조, 애니메이션
- 네비게이션 동작(스크롤 효과, 모바일 토글)
- 기존 CSS 변수 이름과 값
- 프로젝트 카드의 순서, 원문 문구, 링크
- 모바일 반응형 배치

---

## 9. 하지 말 것

- 원문에 없는 성과·수치·기술을 임의로 추가하지 않는다.
- 한 번에 여러 기능을 동시에 바꾸지 않는다. 한 번에 한 가지씩.
- 요청 범위 밖의 파일을 수정하지 않는다.
- 설명 없이 코드만 주지 않는다.

---

## 10. 완료 기준

1. 전체 선택 시 카드 3개, 순서 그대로
2. 웹·데이터·디자인 각각 선택 시 해당 카드만 표시되고 개수 표시가 일치
3. 결과가 없는 경우 안내 문구 노출
4. 카드별 자세히 보기가 독립적으로 열리고 닫힘
5. 다른 섹션의 화면과 동작에 변화 없음
6. 모바일 폭에서 1열 배치가 깨지지 않음

---

## 11. 결정 사항 (2026-09-15)

- 프로젝트 섹션 상단 문구는 **포트폴리오 문구로 교체**한다.
  - 라벨: `프로젝트`
  - 제목: `분야별 프로젝트`
  - 한 줄 소개: `관심 분야를 골라 프로젝트를 확인할 수 있습니다`
- 우측 "확인한 차이" 보조 영역은 **제거**하고, 카드를 가로 3열로 정렬한다.
  (CLAUDE.md의 "카드 3개씩 정렬" 기본 구조와 일치)
- 결과가 0개인 분야를 선택하면 `해당 분야의 프로젝트가 아직 없습니다.` 를 표시한다.
- (2026-09-15 변경) 각 카드에 관련 공식 사이트 링크를 단다. 주소는 실제 접속으로 확인한 것만 쓰고,
  새 탭에서 열리도록 `target="_blank" rel="noopener noreferrer"` 를 함께 넣는다.

---

## 12. 공유 기능 명세

### 위치와 구성
- 네비게이션 오른쪽 끝에 `공유하기` 버튼(`#shareBtn`)을 둔다. 모바일에서는 메뉴 버튼 왼쪽에 놓는다.
- 버튼을 누르면 메뉴(`#shareMenu`)가 열리고 두 항목을 제공한다.
  - `링크 공유` — 현재 페이지 주소를 클립보드에 복사
  - `PDF 공유` — 핵심 내용만 담은 구조화 문서를 인쇄 대화상자로 전달
- 메뉴는 바깥 클릭이나 Esc로 닫는다.

### 링크 공유
- `navigator.clipboard` 를 우선 사용하고, 보안 컨텍스트가 아니면 `execCommand('copy')` 로 대체한다.
- 결과는 화면 아래 토스트(`#toast`)로 알린다.
- `file://` 로 열었을 때는 주소가 아니라 파일 경로가 복사되므로 안내 문구를 다르게 표시한다.

### PDF 공유
- **화면을 그대로 인쇄하지 않는다.** `#pdfDoc` 안에 별도의 문서 구조를 만들고, 인쇄 시 그 문서만 출력한다.
- (2026-09-22 변경) 문서를 만드는 재료를 **DOM 스크래핑에서 `store` 데이터로 바꿨다.**
  백엔드에서 받아 `frontend/js/store.js`에 담아 둔 값을 그대로 쓴다.
  필터로 카드가 다시 그려져도 PDF 내용이 영향을 받지 않는다.
- 인쇄 시 `body > *:not(.pdf-doc)` 를 모두 숨긴다. 배경 SVG, 네비게이션, 장식 요소는 나오지 않는다.
- 문서는 A4 기준(`@page` 여백 16mm/15mm), 이모지와 장식 문자는 제거한 뒤 담는다.

| 문서 구성 | 가져오는 곳 (store 기준) |
|---|---|
| 표지(이름·학과·소개·작성일) | `store.profile.name`, `profile.hero` |
| 기본 정보 | `store.profile.infoTable` |
| 소개 3항목 | `store.profile.about` |
| 학습 역량 + 관심 키워드 | `store.skills.groups`, `store.skills.tags` |
| 프로젝트 3건(분야·설명·키워드·문제/접근/결과·링크) | `store.allProjects` |
| 맺음말 | `store.profile.contact.message` |

- 프로젝트는 **필터로 숨겨진 카드까지 모두** 담는다. 화면의 필터 상태는 PDF에 영향을 주지 않는다.
- 링크는 클릭용이 아니라 읽을 수 있도록 주소 문자열로 함께 적는다.
- 문서는 페이지 로드 시 미리 만들고 `beforeprint` 에서 다시 만든다.
  따라서 버튼을 쓰지 않고 Ctrl+P로 인쇄해도 같은 구조화 문서가 나온다.
- 저장 대화상자의 기본 파일 이름을 위해 인쇄 중에만 `document.title` 을 `이름_포트폴리오` 로 바꾸고,
  `afterprint` 에서 되돌린다.

### 이 기능의 완료 기준
1. 공유하기 버튼을 누르면 두 항목이 있는 메뉴가 열리고, 바깥 클릭·Esc로 닫힌다
2. 링크 공유를 누르면 주소가 복사되고 토스트가 뜬다
3. PDF 공유를 누르면 인쇄 대화상자가 열리고, 미리보기에 화면이 아닌 문서가 보인다
4. 문서에 표지·기본 정보·소개·학습 역량·프로젝트 3건·맺음말이 모두 있다
5. 필터를 웹으로 둔 상태에서 PDF를 뽑아도 프로젝트 3건이 모두 나온다
6. Ctrl+P로 인쇄해도 같은 문서가 나온다

---

## 13. 현재 작업과 다음 단계

1. (완료) 현황 확인 — 파일·기능 읽기
2. (완료) 이 문서 작성 — 분류 기준·동작·디자인 기준 확정
3. (완료) 미확인 항목 결정 — 11절
4. (완료) 섹션 문구 교체, 보조 영역 제거 후 3열 배치, 빈 결과 안내, 개수 표시 연동
5. (완료) 브라우저에서 필터·상세 설명 확인
6. (완료) 공유하기 메뉴와 PDF 추출 기능 추가 — 12절
7. (완료) 프론트엔드/백엔드 분리 — 14절
8. (다음) 인쇄 미리보기에서 12절 완료 기준 6항목 확인

---

## 14. 프론트엔드 / 백엔드 분리 (2026-09-22)

### 왜 나눴나
나중에 데이터베이스나 다른 API를 붙일 때 **고칠 곳을 한곳으로 모으기** 위해서다.
그 전에는 내용이 index.html에 직접 적혀 있어, 항목 하나를 바꾸려면 HTML을 고쳐야 했다.

### 구조

```
portfolio-site/
├── backend/   API 서버 (Node.js + Express)
└── frontend/  화면 (정적 파일 + ES 모듈, 빌드 도구 없음)
```

- 실행: 루트에서 `npm install` 후 `npm run dev` → <http://localhost:4000>
- 개발 중에는 백엔드가 프론트엔드 파일까지 함께 제공한다(`SERVE_FRONTEND=true`).
- 자세한 폴더 설명·API 목록·DB 연결 방법은 `README.md` 에 있다.

### 계층

```
브라우저 ─ fetch ─▶ routes ─▶ controllers ─▶ services ─▶ repositories ─▶ JSON 또는 DB
```

- **repositories** 가 데이터를 실제로 가져오는 유일한 계층이다.
- `backend/src/repositories/index.js` 가 `.env` 의 `DATA_SOURCE` 값(`json` | `db`)을 보고
  구현을 고른다. DB로 바꿀 때 **서비스·컨트롤러·라우트·프론트엔드는 고치지 않는다.**
- `repositories/db/` 에는 MongoDB·PostgreSQL·MySQL 예시가 주석으로 들어간 뼈대가 있다.

### 내용 수정 위치 (중요)

| 바꾸고 싶은 것 | 파일 |
|---|---|
| 이름·소개·연락처·푸터 | `backend/src/data/profile.json` |
| 기술 항목과 퍼센트, 키워드 | `backend/src/data/skills.json` |
| 프로젝트 카드 | `backend/src/data/projects.json` |
| 필터 탭 | `backend/src/data/categories.json` |

**HTML을 고쳐서 내용을 바꾸지 않는다.** `frontend/index.html` 은 빈 뼈대만 유지한다.

### 프론트엔드 파일 분리

기존 `script.js` 한 파일을 역할별로 나눴다.

| 폴더 | 역할 |
|---|---|
| `js/api/` | 백엔드 호출. 화면 코드는 `fetch` 를 직접 쓰지 않는다 |
| `js/render/` | 데이터를 화면 요소로 바꾼다 |
| `js/features/` | 동작 (네비·필터·공유·PDF·등장 효과) |
| `js/lib/` | DOM·토스트 같은 작은 도구 |
| `js/store.js` | 받아온 데이터 보관. PDF는 여기서 꺼내 쓴다 |

### 바뀐 동작 (의도한 변경)

| 항목 | 이전 | 지금 |
|---|---|---|
| 필터 | 카드를 전부 그려 두고 CSS로 숨김 | 누를 때마다 `/api/projects?category=` 요청 |
| PDF 재료 | 화면 DOM을 긁어 모음 | `store` 데이터 사용 |
| 파일 열기 | `index.html` 더블클릭으로도 열림 | **반드시 `npm run dev`로 실행** (ES 모듈·API 호출 때문) |

### 유지된 것
- 화면과 디자인, CSS 변수, 클래스 이름, 반응형 배치
- 필터·상세 보기·공유·PDF의 동작과 문구
- 5절·6절·12절의 동작 명세와 완료 기준

---

## 15. 배포 구조

### 2026-09-22 최초 구성
- 화면: GitHub Pages, 데이터: Vercel 서버리스 함수(`api/index.js`).
- Vercel의 Deployment Protection이 켜져 있어 API가 전부 로그인 화면으로 리다이렉트됨을
  배포 후 실사용 확인 과정에서 발견 — 사용자가 "카드가 안 보인다"고 보고.

### 2026-09-22 변경 — Render로 백엔드 이전
사용자 요청("백엔드는 Render, 배포는 Vercel 위주로")에 따라 재구성.

- 데이터: **Render** (`render.yaml` Blueprint) — `backend/` 를 서버리스가 아니라 계속 켜진
  일반 Node 서버로 그대로 띄움. `backend/src/server.js` 무수정.
- 화면: **Vercel**(`vercel.json` 의 buildCommand로 `frontend/` 복사 후 admin 파일 제외해 정적
  배포) **과 GitHub Pages 둘 다 유지** — 어느 쪽 링크를 써도 같은 내용이 뜨도록.
- `api/index.js`(Vercel 서버리스 진입점)는 제거함 — Render가 API를 전담하므로 불필요.
- `frontend/js/config.js` 의 `PRODUCTION_API_BASE` 를 Render 주소로 변경.
- Render 무료 플랜은 15분 무요청 시 슬립 → 재깨움에 최대 1분 소요. 이를 감안해
  `requestTimeout` 을 로컬 8초 / 배포 45초로 분리하고, 타임아웃 안내 문구도
  "서버를 깨우는 중일 수 있습니다"로 맞춤.
- 관리자 페이지는 여전히 로컬 전용(`ENABLE_ADMIN=false` on Render) — Render 무료 플랜도
  파일 저장이 영구적이지 않으므로(슬립·재배포 시 초기화) 이유가 동일하게 적용됨.
- Render·Vercel 양쪽 모두 대시보드에서 사람이 직접 연결해야 하는 단계가 남아 있음
  (계정 가입, Deployment Protection 해제 등) — 절차는 `README.md` "처음 연결할 때" 참고.
